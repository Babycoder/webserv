#pragma once

#include <fstream>
#include <iostream>
#include <string>
#include <string.h>
#include <stdlib.h>
#include <vector>
#include <map>


std::vector<std::string>        ft_split(std::string str, std::string delim);
bool                            is_empty(std::string str);